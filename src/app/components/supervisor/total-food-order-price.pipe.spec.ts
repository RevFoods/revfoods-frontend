import { TotalFoodOrderPricePipe } from './total-food-order-price.pipe';

describe('TotalFoodOrderPricePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFoodOrderPricePipe();
    expect(pipe).toBeTruthy();
  });
});
